Підтвердження замовлення

Дякуємо за замовлення, <%= @user.email %>!

Ось деталі вашого замовлення:
Адреса доставки: <%= @order.shipping_address %>
<% @order.cart_items.each do |item| %>
  * <%= item.product.title %> - <%= item.quantity %> x <%= number_to_currency(item.price, unit: "₴") %>
<% end %>

Всього: <%= number_to_currency(@order.total_price, unit: "₴") %>
